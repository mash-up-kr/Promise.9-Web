# 실제 값은 .env.local(gitignore)에 두세요. 이 파일은 필요한 키만 문서화합니다.

# 서버 API base URL (예: https://api.link-ding-dong.com)
EXPO_PUBLIC_API_BASE_URL=

# 로그인 연동 전 임시 dev 액세스 토큰(Bearer). 폴더 API 등 인증 필요 엔드포인트 실호출용.
# 정식 로그인 구현(#auth) 시 제거 예정.
EXPO_PUBLIC_DEV_ACCESS_TOKEN=
