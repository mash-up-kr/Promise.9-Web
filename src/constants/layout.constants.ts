/**
 * 웹에서 앱 콘텐츠 컬럼의 최대 폭. 루트 레이아웃(`app/_layout.tsx`)이 이 폭으로 중앙 정렬한다.
 *
 * Modal(팝오버 등)은 이 컬럼 밖 전체 윈도우에 뜨므로, 화면 가장자리 기준 위치를 컬럼
 * 가장자리에 맞추려면 같은 값이 필요하다. 두 곳이 어긋나면 위치 보정이 조용히 깨진다.
 */
export const CONTENT_MAX_WIDTH = 768;
