/** 저장 버튼 아래 안내 — 키보드 Enter 로도 같은 동작을 할 수 있다는 표시(시안 공통). */
export function EnterHint() {
  return (
    <p className="mt-1.5 text-center text-caption-1 text-text-assistive">
      Enter
    </p>
  );
}
